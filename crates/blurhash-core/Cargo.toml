[package]
name = "blurhash-core"
version = "0.1.0"
edition = "2021"
description = "Fast BlurHash encoding/decoding in pure Rust"

[dependencies]
thiserror = "2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "blurhash_bench"
harness = false
